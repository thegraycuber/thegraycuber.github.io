

random uses a better norm for digit counts
make zoom smart in its direction, not always origin at default?
change grid size when menu is hidden
grid and lock together?
copy/paste unlocked and better resistant to bad inputs

delay processing, only get 10 per frame or so
displayLimit upper bound?

write info
add preview to index

show information about int when hovered? 
better digit name alignment

maybe:
	cursor type and color change upon hover in edit pane

other pages:
	add new icons to crabman
